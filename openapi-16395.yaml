# Intentionally insecure fixture for Arnica issue 16395.
openapi: 3.0.0
info:
  title: Arnica IaC closure reproduction
  version: "1.0.0"
servers:
  - url: https://example.com
security:
  - bearerAuth: []
paths:
  /example:
    get:
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
